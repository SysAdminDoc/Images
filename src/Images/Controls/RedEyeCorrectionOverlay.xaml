<UserControl x:Class="Images.Controls.RedEyeCorrectionOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svc="clr-namespace:Images.Services"
             IsHitTestVisible="False"
             Background="Transparent">
    <Grid>
        <Canvas x:Name="OverlayCanvas">
            <ItemsControl ItemsSource="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=Marks}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding Left}" />
                        <Setter Property="Canvas.Top" Value="{Binding Top}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type svc:RedEyeCorrectionMark}">
                        <Grid Width="{Binding Diameter}"
                              Height="{Binding Diameter}"
                              SnapsToDevicePixels="True">
                            <Ellipse Stroke="{StaticResource RedBrush}"
                                     StrokeThickness="2"
                                     StrokeDashArray="2 2"
                                     Fill="#18FF4F64"
                                     Opacity="0.82" />
                            <Ellipse Width="8"
                                     Height="8"
                                     Fill="{StaticResource RedBrush}"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Opacity="0.9" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>
    </Grid>
</UserControl>
